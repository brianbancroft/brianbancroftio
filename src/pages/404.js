import React from 'react'

const NotFoundPage = () => (
  <div>
    <h1>NOT FOUND</h1>
    <p>Sorry, this page doesn't seem to exist. If you believe it does, we should chat.</p>
  </div>
)

export default NotFoundPage
